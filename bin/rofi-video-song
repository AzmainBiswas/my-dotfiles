#!/usr/bin/env bash

# FIND="fd"
FIND="fdfind"
ALL="/mnt/media/video/songs/"

mpv $($FIND . ${ALL} -e mpv -e mp3 -e webm | rofi -dmenu -matching "fuzzy" -p "Videos" -theme ~/.config/rofi/full-theme.rasi)
