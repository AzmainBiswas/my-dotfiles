#!/usr/bin/env bash

video_qu="$1"
video_url="$2"
video_name_raw="${@:3}"
video_name="$(echo ${video_name_raw} | tr ' ' '-' | tr '[A-Z]' '[a-z]' )"

yt-dlp -f "${video_qu}+ba" --merge-output-format mp4 ${video_url} -o "${video_name}"
